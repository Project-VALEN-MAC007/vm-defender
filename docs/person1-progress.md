# สถานะเดิมของโครงการ

ไฟล์นี้คงชื่อเดิมไว้เพื่อให้ลิงก์เก่าไม่เสีย ข้อมูลสถานะปัจจุบันถูกย้ายไปที่
`docs/project-status.md` แล้ว และควรแก้สถานะที่ไฟล์นั้นเพียงแห่งเดียว

หลักฐานการตรวจเครื่องและผลทดสอบเดิมยังเก็บอยู่ใต้ `backups/` และ `evidence/`
โดยไม่แก้ย้อนหลัง
